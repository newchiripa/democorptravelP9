if (typeof taxes === "undefined" || taxes === null || taxes === "") { return; }  
if (typeof currency === "undefined" || currency === null || currency === "") { return; }  
var formattedText = parseFloat(taxes).toFixed(2) + " " + currency; 
return formattedText;