var selectedItem = oEvent.getSource();

if (selectedItem) {
    
    var context = selectedItem.getBindingContext();
    var data = context.getObject();
    
    modeloPageVehicleRate.setData(data);
    
    
    oApp.toDetail(oPageVehicleRate);

}

